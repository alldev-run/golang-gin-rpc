apiVersion: 1

providers:
  - name: "audit"
    orgId: 1
    folder: "Audit"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
